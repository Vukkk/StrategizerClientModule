export const PhaseCode = `
# Phase Code Options\n\n
Phase code options instruct the trading algobot when to make a Take Profit (sell) position or a Stop Loss position.

### Take Profit\n
The Take Profit phase code starts with \`buyOrder = \` which is counter intuitive to the expected \`sellOrder\` because this prototype uses a default strategy built to accumulate BTC. Any of the standard conditional commands can be used.

### In the default simulator algobot...
...to be continued
`;
